# Zeilenenden festnageln.
#
# Windows checkt Textdateien standardmäßig mit CRLF aus. Für die vendorte
# letter-pro-Datei heißt das: andere Bytes, andere Prüfsumme — und der Test,
# der ihre Unversehrtheit belegt, schlägt dort immer fehl. Gemessen in der CI
# am 25.08.2026.
skill/falzmarke/typst/vendor/letter-pro-v3.0.0.typ -text

# Alles andere einheitlich mit LF im Repository.
* text=auto eol=lf
*.otf binary
*.png binary
*.pdf binary
